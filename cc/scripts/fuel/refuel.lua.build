function MODULE_entrypoints_scripts_fuel_refuel()
	for i = 1, 16 do
	    turtle.select(i)
	    if turtle.refuel(0) then
	        turtle.refuel()
	    end
	end
	print("Fuel: " .. turtle.getFuelLevel())
end
local entrypoints_scripts_fuel_refuel = MODULE_entrypoints_scripts_fuel_refuel()